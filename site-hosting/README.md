# Acoustic Lab hosting wrapper

This minimal Next application packages the static browser tools in `website/`
for OpenAI Sites. Before building or publishing, copy the current `website/`
contents into this directory's `public/` folder and copy
`.openai/hosting.json` into `.openai/hosting.json`.
